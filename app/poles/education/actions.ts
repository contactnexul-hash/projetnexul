"use server"; // Indique que c'est une Server Action

export async function submitVolunteer(action: string) {
  // Ici tu peux envoyer les données à ton serveur ou API
  console.log("Volunteer action submitted:", action);
}
