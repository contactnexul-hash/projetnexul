"use server"; // nécessaire pour les Server Actions

export async function submitCharity(action: string) {
  console.log("Charity action submitted:", action);
  // ici tu peux ajouter un envoi vers ta base de données ou API
}

